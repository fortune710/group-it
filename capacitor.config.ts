import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.groupit.app',
  appName: 'group-it',
  webDir: 'dist/example-app',
  bundledWebRuntime: false
};

export default config;
